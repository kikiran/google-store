apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: nova-store
  labels:
    app: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: notification-service
          image: novastoreacr.azurecr.io/nova/notification-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: nova-store-config
            - secretRef:
                name: nova-store-secrets
          env:
            - name: PORT_NOTIFICATION
              value: "3011"
          ports:
            - name: http
              containerPort: 3011
          livenessProbe:
            tcpSocket:
              port: 3011
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            tcpSocket:
              port: 3011
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: nova-store
spec:
  selector:
    app: notification-service
  ports:
    - port: 3011
      targetPort: 3011
