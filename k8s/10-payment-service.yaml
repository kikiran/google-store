apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: nova-store
  labels:
    app: payment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: payment-service
          image: novastoreacr.azurecr.io/nova/payment-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: nova-store-config
            - secretRef:
                name: nova-store-secrets
          env:
            - name: PORT_PAYMENT
              value: "3007"
          ports:
            - name: http
              containerPort: 3007
          livenessProbe:
            tcpSocket:
              port: 3007
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            tcpSocket:
              port: 3007
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: nova-store
spec:
  selector:
    app: payment-service
  ports:
    - port: 3007
      targetPort: 3007
