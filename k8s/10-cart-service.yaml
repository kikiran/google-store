apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: nova-store
  labels:
    app: cart-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: cart-service
          image: novastoreacr.azurecr.io/nova/cart-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: nova-store-config
            - secretRef:
                name: nova-store-secrets
          env:
            - name: PORT_CART
              value: "3005"
          ports:
            - name: http
              containerPort: 3005
          livenessProbe:
            tcpSocket:
              port: 3005
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            tcpSocket:
              port: 3005
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: nova-store
spec:
  selector:
    app: cart-service
  ports:
    - port: 3005
      targetPort: 3005
