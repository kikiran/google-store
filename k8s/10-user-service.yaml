apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: nova-store
  labels:
    app: user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
        - name: user-service
          image: novastoreacr.azurecr.io/nova/user-service:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: nova-store-config
            - secretRef:
                name: nova-store-secrets
          env:
            - name: PORT_USER
              value: "3002"
          ports:
            - name: http
              containerPort: 3002
          livenessProbe:
            tcpSocket:
              port: 3002
            initialDelaySeconds: 20
            periodSeconds: 20
          readinessProbe:
            tcpSocket:
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: nova-store
spec:
  selector:
    app: user-service
  ports:
    - port: 3002
      targetPort: 3002
